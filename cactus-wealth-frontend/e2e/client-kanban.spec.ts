import { test, expect } from '@playwright/test';

test.describe('@smoke Client Kanban Flow', () => {
  test('should add a client and move in Kanban', async ({ page }) => {
    // TODO: implementar flujo real
    expect(true).toBe(true);
  });
}); 